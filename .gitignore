# This repo holds a PRE-BUILT static artifact — the committed files ARE what the
# BlueStep GitSite serves under /spa/. The build output (index.html, _next/,
# *.txt RSC payloads, favicon, per-route folders) MUST stay committed — do not
# add any of it here.

# OS / editor cruft only
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
